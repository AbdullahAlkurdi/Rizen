{
  "name": "rizen",
  "version": "1.0.0",
  "description": "AI-powered Life Operating System",
  "private": true,
  "scripts": {
    "build": "flutter build web --release",
    "build:prod": "flutter build web --release",
    "deploy": "firebase deploy --only hosting",
    "build-deploy": "npm run build && npm run deploy"
  }
}
